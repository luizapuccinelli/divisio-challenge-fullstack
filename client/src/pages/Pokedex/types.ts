export interface PokemonType {
    id: string
    name: string
    image: string
    maxHP: number
    maxCP: number
    number: number
    types: string[]
    weaknesses: string[]
    resistant: string []
}